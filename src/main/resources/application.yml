server:
  port: 8888
spring:
  application:
    name: reto-autentia
  datasource:
    driverClassName: com.mysql.cj.jdbc.Driver
    url: "jdbc:mysql://localhost:3306/rau"
    #url: {{ SPRING_DATASOURCE_URL }}
    username: root
    password: admin
  jpa:
    show-sql: true
    defer-datasource-initialization: true
    hibernate:
      ddl-auto: create-drop